apiVersion: v1
kind: Service
metadata:
  name: {{ include "langchain-agent.fullname" . }}
  labels:
    {{- include "langchain-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "langchain-agent.selectorLabels" . | nindent 4 }}